<div class="booking-form-container">
  <div class="row justify-content-center">
    <div class="col-12 col-med-8">
      <%# <% @service.bookings.each do |booking| %>
        <%# <% booking = Booking.find_by(service: @service, booking: booking) %>
        <div class="booking-form-title">
          <h3><%= current_user.email %>, book the service!</h3>
        </div>
        <div class="booking-form-new">
          <%= simple_form_for [@service, @booking] do |f| %>
            <%= f.input :start_date, label: "Start Date", as: :string, required: false, input_html: {class: "datepicker"} %>
            <%= f.submit "Book!", class: "btn btn-primary" %>
          <% end %>
        </div>
        <div style="margin-top: 25px; margin-bottom:25px">
          <hr>
        </div>
        <div class="booking-show-host-info">
          <p>Hosted by ....</p>
        </div>
      <%# <% end %>
    </div>
  </div>
</div>
